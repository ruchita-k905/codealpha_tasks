developed a language translation tool using Streamlit for the UI and Google Translator API via the deep-translator library. The app allows users to select source and target languages, translate text in real time, and copy the translated output for usability.
to run file 
# python -m streamlit run translator_app.py
